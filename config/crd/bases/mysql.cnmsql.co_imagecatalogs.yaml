---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: imagecatalogs.mysql.cnmsql.co
spec:
  group: mysql.cnmsql.co
  names:
    kind: ImageCatalog
    listKind: ImageCatalogList
    plural: imagecatalogs
    shortNames:
    - myimagecatalog
    singular: imagecatalog
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ImageCatalog is the Schema for the imagecatalogs API (namespaced).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ImageCatalog
            properties:
              images:
                description: |-
                  Images is the list of major version to container image mappings. Each
                  major version must appear at most once.
                items:
                  description: CatalogImage maps a MySQL major version to a container
                    image.
                  properties:
                    image:
                      description: Image is the fully qualified Percona Server for
                        MySQL image reference.
                      type: string
                    major:
                      description: |-
                        Major is the MySQL major version (e.g. 8 for 8.0/8.4 lines uses the full
                        version where needed; values map to the image's server version).
                      minimum: 5
                      type: integer
                  required:
                  - image
                  - major
                  type: object
                maxItems: 8
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - major
                x-kubernetes-list-type: map
            required:
            - images
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
