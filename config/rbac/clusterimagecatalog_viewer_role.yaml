# This rule is not used by the project cnmysql itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to mysql.cloudnative-mysql.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: cnmysql
    app.kubernetes.io/managed-by: kustomize
  name: clusterimagecatalog-viewer-role
rules:
- apiGroups:
  - mysql.cloudnative-mysql.io
  resources:
  - clusterimagecatalogs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - mysql.cloudnative-mysql.io
  resources:
  - clusterimagecatalogs/status
  verbs:
  - get
